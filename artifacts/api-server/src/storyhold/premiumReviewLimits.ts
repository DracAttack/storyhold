/** Shared immutable execution bounds. Kept dependency-free so plan and journal
 * integrity code can validate the same contract without an import cycle. */
export const PREMIUM_CLOCK_PAGES_PER_VERIFICATION_BATCH_LIMIT = 4;
